# commit-code

**Reference copy.** The lab ships the active command at [`.cursor/commands/commit-code.md`](../../.cursor/commands/commit-code.md).

Optional arguments: `--quick` or `--no-review` (skip the expensive sub-agent review step).

See WORKFLOW_RECIPES.md Recipe 3 for the framework.
